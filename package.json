{
  "name": "@hexlet/code",
  "version": "1.0.0",
  "description": "Чат приложение, рерайт чужого проекта.",
  "main": "index.js",
  "scripts": {
    "build": "cd frontend && npm ci && npm run build",
    "start": "npm run build && npx serve -s frontend/dist -l 10000",
    "heroku-postbuild": "cd frontend && npm ci && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/yourname/hexlet-clone.git"
  },
  "author": "yourname",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/yourname/hexlet-clone/issues"
  },
  "homepage": "https://github.com/yourname/hexlet-clone#readme",
  "dependencies": {
    "@hexlet/chat-server": "^2.0.4",
    "leo-profanity": "^1.7.0",
    "serve": "^14.2.0"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
