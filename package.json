{
  "name": "hexlet-clone",
  "version": "1.0.0",
  "description": "Чат приложение, рерайт чужого проекта.",
  "main": "index.js",
  "scripts": {
    "build": "cd frontend && npm run build",
    "start": "npm run build && start-server -s ./frontend/dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd frontend && npm ci"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/yourname/hexlet-clone.git"
  },
  "author": "yourname",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/yourname/hexlet-clone/issues"
  },
  "homepage": "https://github.com/yourname/hexlet-clone#readme",
  "dependencies": {
    "@hexlet/chat-server": "^2.0.4"
  }
}
